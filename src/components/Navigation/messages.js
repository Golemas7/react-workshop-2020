import { defineMessages } from "react-intl";
const scope = "components.navigation.";

export default defineMessages({
  linkHome: {
    id: `${scope}linkHome`,
    defaultMessage: "Home",
  },
  linkRestaurantList: {
    id: `${scope}restaurantList`,
    defaultMessage: "Restaurant list",
  },
});
